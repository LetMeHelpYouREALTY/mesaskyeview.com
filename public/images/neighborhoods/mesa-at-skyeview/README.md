# Mesa at Skyeview photos

All live imagery is served from **Cloudflare-cached** `public/Image/*` paths.

See `lib/site-images.ts` and `lib/mesaskyeview-photos.ts` for URLs and alt text. Do not duplicate large JPGs in this folder.
